#include "libft.h"

int	main(void)
{
	void	*p;
	char *s = ft_strdup((char *)"coucou");
	printf("%s\n",s);
	return (0);
}

        // char * s = ft_strdup((char*)"coucou");
        // 1  check(!strcmp(s, "coucou"));
